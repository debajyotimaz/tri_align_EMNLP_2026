# Core ML / NLP
torch>=2.0.0
transformers>=4.38.0
datasets>=2.18.0

# Numerical & data
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Retrieval
faiss-cpu>=1.7.4         # use faiss-gpu if running on a CUDA machine

# Interpretability & visualization
matplotlib>=3.7.0
seaborn>=0.13.0

# Utilities
tqdm>=4.66.0
pyyaml>=6.0              # for Translation YAML configs
openpyxl>=3.1.0          # for downstream task Excel result saving
pyarrow>=14.0.0          # for reading .parquet corpus files

# Translation pipeline (requires a GPU server)
# vllm>=0.4.0            # uncomment only on the vLLM inference server
